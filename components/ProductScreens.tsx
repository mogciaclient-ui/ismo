"use client";

import { useEffect, useMemo, useState } from "react";
import { ArrowRight, Check, Plus, Sparkle, Trash } from "@phosphor-icons/react";
import { analyticsProvider, type OverviewSnapshot, type SiteSettings } from "@/lib/analytics";
import { getLast30DaysRange } from "@/lib/date-range";
import { useSiteWorkspace } from "@/lib/site-workspace";
import { BrandLoader } from "@/components/BrandLoader";

const channels = ["Organic Search", "SNS", "Web広告", "LINE", "Referral", "Direct", "その他"];
const goals = ["問い合わせ", "資料請求", "LINE追加", "予約", "購入", "電話", "店舗来店", "SNS", "その他"];
const siteRoles = [
  { name: "信頼獲得", description: "会社やサービスへの安心感をつくる", focus: "会社情報・実績・事例が十分に伝わっているか" },
  { name: "問い合わせ獲得", description: "相談や資料請求につなげる", focus: "CTAへの到達率とコンバージョン率" },
  { name: "採用", description: "応募前の理解と共感を深める", focus: "仕事内容・人・文化から応募への導線" },
  { name: "予約・購入", description: "予約や商品の購入につなげる", focus: "検討ページから完了までの離脱と成果率" },
  { name: "営業支援", description: "商談前後の説明を補助する", focus: "サービス・料金・事例の閲覧状況" },
  { name: "SNSの受け皿", description: "SNSで興味を持った人を受け止める", focus: "SNS別の流入、関心ページ、次の行動" },
  { name: "情報発信", description: "専門性や最新情報を届ける", focus: "検索流入、記事閲覧、再訪問の傾向" },
  { name: "顧客サポート", description: "疑問を解消し利用を助ける", focus: "案内ページの到達と自己解決につながる行動" },
];

function useSiteSettings() {
  const { selectedSiteId } = useSiteWorkspace();
  const [settings, setSettings] = useState<SiteSettings | null>(null);
  const [saving, setSaving] = useState(false);
  useEffect(() => { setSettings(null); analyticsProvider.getSiteSettings(selectedSiteId).then(setSettings); }, [selectedSiteId]);
  const save = async (next: SiteSettings) => { setSaving(true); await analyticsProvider.saveSiteSettings(next); setSettings(next); setSaving(false); };
  return { settings, setSettings, save, saving, selectedSiteId };
}

function ScreenHead({ eyebrow, title, sub, action }: { eyebrow: string; title: string; sub: string; action?: React.ReactNode }) {
  return <div className="page-head"><div><p className="eyebrow">{eyebrow}</p><h1>{title}</h1><p className="sub">{sub}</p></div>{action}</div>;
}

function Loading() { return <BrandLoader label="分析データを読み込んでいます" />; }

export function StrategyScreen() {
  const { settings, setSettings, save, saving } = useSiteSettings();
  const { canEdit, role } = useSiteWorkspace();
  if (!settings) return <Loading />;
  const strategy = settings.strategy ?? { siteRoles: [], audience: "", businessType: "BtoB" as const, userProblem: "", considerationStage: "", entryChannels: [], goals: [], siteRole: "", expectedJourney: "" };
  const update = (patch: Partial<typeof strategy>) => setSettings({ ...settings, strategy: { ...strategy, ...patch } });
  const toggle = (key: "entryChannels" | "goals", value: string) => update({ [key]: strategy[key].includes(value) ? strategy[key].filter(item => item !== value) : [...strategy[key], value] });
  const selectedRoles = strategy.siteRoles ?? [];
  const toggleRole = (role: string) => { if (selectedRoles.includes(role)) update({ siteRoles: selectedRoles.filter(item => item !== role) }); else if (selectedRoles.length < 3) update({ siteRoles: [...selectedRoles, role] }); };
  const suggest = () => { const analysis = settings.siteAnalysis; if (!analysis) return; update({ audience: strategy.audience || analysis.target, siteRole: strategy.siteRole || analysis.mainMessage, userProblem: strategy.userProblem || analysis.recommendations[0] || "", entryChannels: strategy.entryChannels.length ? strategy.entryChannels : ["Organic Search", "SNS"], goals: strategy.goals.length ? strategy.goals : ["問い合わせ"], expectedJourney: strategy.expectedJourney || `${analysis.pages?.[0]?.title ?? "TOP"} → CTA → 問い合わせ` }); };
  const strategySummary = `${selectedRoles.length ? `${selectedRoles.join("・")}を担うサイトとして、` : ""}${strategy.audience || "届けたい相手"}へ、${strategy.siteRole || "サイトで伝える価値"}を届け、${strategy.entryChannels.length ? strategy.entryChannels.join("・") : "主な流入経路"}から${strategy.goals.length ? strategy.goals.join("・") : "成果"}につなげる。`;
  return <>
    <ScreenHead eyebrow="PLAN" title="サイトの目的・ターゲット" sub="4つの質問に答えて、分析と改善の判断基準をつくります。" action={canEdit && settings.siteAnalysis ? <button className="primary-action" type="button" onClick={suggest}><Sparkle />分析結果から候補を入れる</button> : undefined} />
    <div className="strategy-guide"><div><span>01</span><b>誰に</b></div><ArrowRight /><div><span>02</span><b>何を</b></div><ArrowRight /><div><span>03</span><b>どこから</b></div><ArrowRight /><div><span>04</span><b>どの成果へ</b></div></div>
    {!canEdit && <div className="readonly-note">{role === "agency" ? "代理店" : "クライアント"}権限では閲覧のみです。変更はMOGCIAへ相談してください。</div>}
    <fieldset className="editable-area" disabled={!canEdit}><section className="panel strategy-role-diagnosis"><div className="strategy-role-head"><div><p className="eyebrow">SITE ROLE DIAGNOSIS</p><h2>このサイトに、どんな仕事をしてほしい？</h2><p>一番大切な役割から順に、最大3つまで選んでください。</p></div><span>{selectedRoles.length} / 3</span></div><div className="strategy-role-grid">{siteRoles.map(item => { const selectedIndex = selectedRoles.indexOf(item.name); return <button type="button" className={selectedIndex >= 0 ? "selected" : ""} aria-pressed={selectedIndex >= 0} key={item.name} onClick={() => toggleRole(item.name)}><i>{selectedIndex >= 0 ? selectedIndex + 1 : ""}</i><div><b>{item.name}</b><span>{item.description}</span></div>{selectedIndex >= 0 && <Check />}</button>; })}</div>{selectedRoles.length ? <div className="strategy-role-result"><Sparkle weight="fill" /><div><span>診断結果</span><h3>主な役割：{selectedRoles[0]}</h3><p>{siteRoles.find(item => item.name === selectedRoles[0])?.focus}を中心に分析します。{selectedRoles.length > 1 ? `あわせて「${selectedRoles.slice(1).join("・")}」も評価します。` : ""}</p></div></div> : <div className="strategy-role-empty">役割を選ぶと、ismoが優先して見るポイントを表示します。</div>}</section><div className="strategy-guide"><div><span>01</span><b>誰に</b></div><ArrowRight /><div><span>02</span><b>何を</b></div><ArrowRight /><div><span>03</span><b>どこから</b></div><ArrowRight /><div><span>04</span><b>どの成果へ</b></div></div><div className="strategy-simple-grid"><section className="panel strategy-question"><em>01 / AUDIENCE</em><h2>誰に見てほしい？</h2><p>このサイトで一番助けたい相手を書きます。</p><label><span>想定する相手</span><textarea value={strategy.audience} onChange={event => update({ audience: event.target.value })} placeholder="例：Webからの問い合わせを増やしたい中小企業の経営者" /></label></section><section className="panel strategy-question"><em>02 / MESSAGE</em><h2>何を伝えたい？</h2><p>相手に覚えてほしい価値や強みを書きます。</p><label><span>サイトで伝えること</span><textarea value={strategy.siteRole} onChange={event => update({ siteRole: event.target.value })} placeholder="例：企画から運用まで一気通貫で支援できる" /></label></section><section className="panel strategy-question"><em>03 / ENTRY</em><h2>どこから来てもらう？</h2><p>想定する主な入口を選びます。複数選択できます。</p><div className="choice-grid">{channels.map(item => <button type="button" className={strategy.entryChannels.includes(item) ? "selected" : ""} key={item} onClick={() => toggle("entryChannels", item)}>{strategy.entryChannels.includes(item) && <Check />}{item}</button>)}</div></section><section className="panel strategy-question"><em>04 / GOAL</em><h2>最後に何をしてほしい？</h2><p>サイトの成果として計測したい行動を選びます。</p><div className="choice-grid">{goals.map(item => <button type="button" className={strategy.goals.includes(item) ? "selected" : ""} key={item} onClick={() => toggle("goals", item)}>{strategy.goals.includes(item) && <Check />}{item}</button>)}</div></section></div><details className="panel strategy-advanced"><summary>さらに詳しく設定する</summary><div className="form-grid"><label><span>ビジネス種別</span><select value={strategy.businessType} onChange={event => update({ businessType: event.target.value as typeof strategy.businessType })}><option>BtoB</option><option>BtoC</option><option>Both</option></select></label><label><span>ユーザーが抱える課題</span><textarea value={strategy.userProblem} onChange={event => update({ userProblem: event.target.value })} placeholder="相手が解決したい悩み" /></label><label><span>検討段階</span><input value={strategy.considerationStage} onChange={event => update({ considerationStage: event.target.value })} placeholder="情報収集 / 比較検討 / 購入直前" /></label><label><span>想定する閲覧導線</span><input value={strategy.expectedJourney} onChange={event => update({ expectedJourney: event.target.value })} placeholder="SNS → LP → 料金 → 問い合わせ" /></label></div></details></fieldset><section className="strategy-summary"><Sparkle weight="fill" /><div><span>このサイトの方針</span><p>{strategySummary}</p></div></section>
    {canEdit && <div className="settings-actions"><button disabled={saving} onClick={() => save(settings)}>{saving ? "保存中…" : "目的・ターゲットを保存"}</button></div>}
  </>;
}

export function SiteAnalysisScreen() {
  const { settings, setSettings } = useSiteSettings();
  const [running, setRunning] = useState(false);
  const [error, setError] = useState("");
  if (!settings) return <Loading />;
  const analysis = settings.siteAnalysis;
  const run = async () => { setRunning(true); setError(""); try { await analyticsProvider.analyzeSite(settings.id); setSettings(await analyticsProvider.getSiteSettings(settings.id)); } catch (cause) { const code = typeof cause === "object" && cause && "code" in cause ? String(cause.code) : ""; setError(code.includes("permission-denied") ? "このサイトを分析できるのはMOGCIA権限だけです。" : code.includes("unauthenticated") ? "認証を確認できませんでした。再ログインしてください。" : "サイト分析に失敗しました。サイトURLとOpenAI APIの利用状況を確認してください。"); } finally { setRunning(false); } };
  const overview = [{ en: "SITE SUMMARY", ja: "サイト概要", text: analysis?.summary }, { en: "MAIN MESSAGE", ja: "メイン訴求", text: analysis?.mainMessage }, { en: "TARGET", ja: "想定ターゲット", text: analysis?.target }];
  const diagnostics = [{ title: "セクション構成", label: "STRUCTURE", items: analysis?.sections ?? [] }, { title: "訴求・強み", label: "MESSAGING", items: analysis?.strengths ?? [] }, { title: "CTA", label: "CALL TO ACTION", items: analysis?.ctas ?? [] }];
  const history = settings.analysisHistory ?? [];
  return <><ScreenHead eyebrow="UNDERSTAND" title="Site Analysis" sub="現在のWebサイトを、登録したStrategyに照らして一画面で読み解きます。" action={<button className="primary-action" onClick={run} disabled={running}><Sparkle />{running ? "分析中…" : analysis ? "再分析" : "サイトを分析"}</button>} />{error && <div className="inline-error">{error}</div>}<div className="site-analysis-board"><section className="panel analysis-result analysis-overview"><div className="panel-head"><h3>サイト全体</h3><span>OVERVIEW</span></div>{analysis ? overview.map(item => <article key={item.en}><span><small>{item.en}</small>{item.ja}</span><p>{item.text}</p></article>) : <div className="empty-state">サイト分析を実行すると表示されます</div>}</section><section className="panel analysis-side"><span>AI EVALUATION</span><h3>{settings.strategy?.audience ? "Strategy登録済み" : "Strategyが未登録です"}</h3><p>{analysis?.recommendations?.[0] ?? (settings.strategy?.audience ? "サイトを分析すると、登録したStrategyに対する評価を表示します。" : "先にWHO・FROM・WHY・GOALを登録すると、目的に対する評価ができます。")}</p></section><section className="panel analysis-result analysis-pages"><div className="panel-head"><h3>主要ページ</h3><span>PAGES</span></div>{analysis?.pages?.length ? analysis.pages.map((item, index) => <article key={item.url}><span>{String(index + 1).padStart(2, "0")}</span><p><b>{item.title}</b><small>{item.url}</small>{item.summary}</p></article>) : <div className="empty-state">主要ページはまだ分析されていません</div>}</section>{diagnostics.map(section => <section className="panel analysis-result analysis-diagnostic" key={section.label}><div className="panel-head"><h3>{section.title}</h3><span>{section.label}</span></div>{section.items.length ? section.items.map((item, index) => <article key={`${item}-${index}`}><span>{String(index + 1).padStart(2, "0")}</span><p>{item}</p></article>) : <div className="empty-state">分析結果がありません</div>}</section>)}<section className="panel analysis-history analysis-history-wide"><div className="panel-head"><h3>分析履歴</h3><span>SNAPSHOTS</span></div>{history.length ? history.slice(-4).reverse().map((item, index) => <article key={`${item.analyzedAt}-${index}`}><time>{new Date(item.analyzedAt).toLocaleDateString("ja-JP")}</time><p>{item.summary}</p></article>) : <div className="empty-state">分析履歴はまだありません</div>}</section></div></>;
}

export function CompetitorsScreen() {
  const { settings, setSettings, save, saving } = useSiteSettings();
  const { canEdit } = useSiteWorkspace();
  const [running, setRunning] = useState(false);
  const [draft, setDraft] = useState({ name: "", url: "", siteType: "HP", note: "" });
  if (!settings) return <Loading />;
  const competitors = settings.competitors ?? [];
  const add = () => { if (!draft.name.trim() || !draft.url.trim() || competitors.length >= 5) return; setSettings({ ...settings, competitors: [...competitors, { id: crypto.randomUUID(), ...draft }] }); setDraft({ name: "", url: "", siteType: "HP", note: "" }); };
  const analyze = async () => { setRunning(true); await save(settings); try { await analyticsProvider.analyzeCompetitors(settings.id); setSettings(await analyticsProvider.getSiteSettings(settings.id)); } finally { setRunning(false); } };
  const result = settings.competitorAnalysis;
  return <><ScreenHead eyebrow="UNDERSTAND" title="Competitors" sub="自社と競合3〜5サイトを比較し、差別化の余白を探します。" />{!canEdit && <div className="readonly-note">競合の追加・再分析はMOGCIAが行います。</div>}<div className="competitor-layout"><section className="panel competitor-list"><div className="panel-head"><h3>競合サイト</h3><span>{competitors.length} / 5</span></div>{competitors.map(item => <article key={item.id}><div><b>{item.name}</b><span>{item.url}</span></div><em>{item.siteType}</em>{canEdit && <button aria-label={`${item.name}を削除`} onClick={() => setSettings({ ...settings, competitors: competitors.filter(row => row.id !== item.id) })}><Trash /></button>}</article>)}{canEdit && <><div className="competitor-add"><input value={draft.name} placeholder="競合企業名" onChange={event => setDraft({ ...draft, name: event.target.value })} /><input value={draft.url} placeholder="https://competitor.example" onChange={event => setDraft({ ...draft, url: event.target.value })} /><select value={draft.siteType} onChange={event => setDraft({ ...draft, siteType: event.target.value })}><option>HP</option><option>LP</option><option>サービスサイト</option></select><button onClick={add}><Plus />追加</button></div><div className="settings-actions"><button disabled={saving} onClick={() => save(settings)}>{saving ? "保存中…" : "競合を保存"}</button><button className="pink-action" disabled={running || !competitors.length} onClick={analyze}><Sparkle />{running ? "比較中…" : "差別化を分析"}</button></div></>}</section><aside><section className="panel differentiation"><p className="eyebrow">DIFFERENTIATION</p>{(["common", "weakness", "strength", "opportunity"] as const).map(key => <div key={key}><span>{key.toUpperCase()}</span><p>{result?.[key]?.join(" / ") || "未分析"}</p></div>)}{result?.recommendation && <blockquote>{result.recommendation}</blockquote>}</section>{result?.positioning?.length ? <section className="panel positioning"><span>POSITIONING</span><div>{result.positioning.map(point => <i key={point.name} title={point.name} style={{ left: `${Math.max(5, Math.min(95, point.x))}%`, bottom: `${Math.max(5, Math.min(95, point.y))}%` }}><b>{point.name}</b></i>)}</div></section> : null}</aside></div></>;
}

export function ImproveScreen() {
  const { settings, setSettings, save, saving } = useSiteSettings();
  const [title, setTitle] = useState("");
  if (!settings) return <Loading />;
  const improvements = settings.improvements ?? [];
  const logs = settings.changeLog ?? [];
  const addIdea = (idea: string, evidence = "AI分析") => { if (!idea.trim() || improvements.some(item => item.title === idea.trim())) return; setSettings({ ...settings, improvements: [...improvements, { id: crypto.randomUUID(), title: idea.trim(), page: "/", problem: idea.trim(), evidence, proposal: idea.trim(), priority: "Medium", status: "提案", createdAt: new Date().toISOString() }] }); };
  const add = () => { addIdea(title); setTitle(""); };
  const nextStatus = (id: string) => { const order = ["提案", "承認", "対応中", "公開", "検証"] as const; const item = improvements.find(row => row.id === id)!; const next = order[Math.min(order.indexOf(item.status) + 1, order.length - 1)]; const updated = improvements.map(row => row.id === id ? { ...row, status: next } : row); const changeLog = next === "公開" && item.status !== "公開" ? [...logs, { id: crypto.randomUUID(), date: new Date().toISOString().slice(0, 10), title: item.title, reason: item.problem || item.proposal, result: "検証待ち" }] : logs; setSettings({ ...settings, improvements: updated, changeLog }); };
  const ideas = [{ type: "OPPORTUNITY", text: settings.siteAnalysis?.recommendations?.[0] }, { type: "DIFFERENTIATION", text: settings.competitorAnalysis?.recommendation }];
  return <><ScreenHead eyebrow="ACT" title="Improve" sub="Insightから改善、公開、結果検証までを一つの流れで管理します。" /><div className="improve-overview"><section className="panel insight-list"><div className="panel-head"><h3>改善候補</h3><span>INSIGHTS</span></div>{ideas.map(idea => <article key={idea.type}><Sparkle /><div><span>{idea.type}</span><h3>{idea.text ?? `${idea.type === "OPPORTUNITY" ? "Site Analysis" : "Competitors"}を実行すると表示されます。`}</h3></div>{idea.text && <button onClick={() => addIdea(idea.text!, idea.type)}>改善に追加<ArrowRight /></button>}</article>)}</section><section className="panel improvement-board"><div className="panel-head"><h3>改善施策</h3><span>{improvements.length} ITEMS</span></div><div className="improvement-add"><input value={title} onChange={event => setTitle(event.target.value)} placeholder="改善施策を追加" /><button onClick={add}><Plus />追加</button></div>{improvements.length ? improvements.map(item => <article key={item.id}><span className={`priority ${item.priority.toLowerCase()}`}>{item.priority}</span><div><h3>{item.title}</h3><p>{item.page} ・ {new Date(item.createdAt).toLocaleDateString("ja-JP")}</p></div><button onClick={() => nextStatus(item.id)}>{item.status}<ArrowRight /></button></article>) : <div className="empty-state">改善施策はまだありません</div>}</section><section className="panel change-log"><div className="panel-head"><h3>変更履歴</h3><span>CHANGE LOG</span></div>{logs.length ? logs.map(item => <article key={item.id}><time>{item.date}</time><div><h3>{item.title}</h3><p>{item.reason || "変更理由は未登録です"}</p></div><strong>{item.result}</strong></article>) : <div className="empty-state">公開された改善はまだありません</div>}</section></div><div className="settings-actions"><button disabled={saving} onClick={() => save(settings)}>{saving ? "保存中…" : "Improveを保存"}</button></div></>;
}

export function PerformanceDetailScreen({ view }: { view: "Segments" | "Funnel" | "Data Quality" }) {
  const { selectedSiteId } = useSiteWorkspace();
  const [data, setData] = useState<OverviewSnapshot | null>(null);
  const [settings, setSettings] = useState<SiteSettings | null>(null);
  useEffect(() => { Promise.all([analyticsProvider.getOverview(selectedSiteId, getLast30DaysRange()), analyticsProvider.getSiteSettings(selectedSiteId)]).then(([overview, site]) => { setData(overview); setSettings(site); }); }, [selectedSiteId]);
  if (!data || !settings) return <Loading />;
  if (view === "Segments") return <><ScreenHead eyebrow="USER INSIGHT" title="デバイス別ユーザー" sub="端末ごとの差から、改善を優先する利用環境を見つけます。" /><section className="segment-grid">{data.deviceSegments.map(row => <article className="panel" key={row.name}><span>{row.name.toUpperCase()}</span><strong>{row.sessions.toLocaleString()}</strong><small>sessions</small><p>CV {row.outcomes} ・ CVR {row.rate}%</p></article>)}</section></>;
  if (view === "Funnel") { const planned = settings.strategy?.expectedJourney?.split(/\s*[→>]\s*/).filter(Boolean) ?? []; const actual = data.journeys[0]?.pages ?? []; return <><ScreenHead eyebrow="JOURNEY COMPARE" title="想定導線と実績" sub="制作時に想定した導線と、実際によく通る経路を並べます。" /><div className="funnel-compare"><section className="panel"><span>PLANNED</span>{planned.length ? planned.map((step, i) => <article key={step}><i>{i + 1}</i><b>{step}</b></article>) : <div className="empty-state">Strategyで想定Journeyを登録してください</div>}</section><section className="panel"><span>ACTUAL</span>{actual.length ? actual.map((step, i) => <article key={`${step.name}-${i}`}><i>{i + 1}</i><b>{step.name}</b><em>{step.sessions} sessions</em></article>) : <div className="empty-state">経路データがまだありません</div>}</section></div></> }
  const q = data.dataQuality; return <><ScreenHead eyebrow="MEASUREMENT STATUS" title="計測状態" sub="判断に使う前に、計測データの鮮度と抜けを確認します。" /><div className="quality-grid"><article className="panel last-event-card"><span>LAST EVENT</span><strong>{q.lastEventAt ? new Date(q.lastEventAt).toLocaleString("ja-JP") : "未受信"}</strong></article><article className="panel"><span>EVENTS</span><strong>{q.eventCount.toLocaleString()}</strong></article><article className="panel"><span>TAGGED PAGES</span><strong>{q.taggedPages}</strong></article><article className="panel"><span>ATTRIBUTION</span><strong>{q.attributionCoverage}%</strong></article></div><section className={`panel quality-status ${q.hasConversions ? "good" : "attention"}`}><Sparkle /><div><span>{q.hasConversions ? "READY" : "ATTENTION"}</span><h3>{q.hasConversions ? "コンバージョンを含むデータを取得できています。" : "コンバージョンイベントがまだありません。ゴール設定とタグを確認してください。"}</h3></div></section></>;
}

export function ClientViewScreen() {
  const { selectedSiteId, selectedSite } = useSiteWorkspace();
  const [data, setData] = useState<OverviewSnapshot | null>(null);
  const [settings, setSettings] = useState<SiteSettings | null>(null);
  useEffect(() => { Promise.all([analyticsProvider.getOverview(selectedSiteId, getLast30DaysRange()), analyticsProvider.getSiteSettings(selectedSiteId)]).then(([overview, site]) => { setData(overview); setSettings(site); }); }, [selectedSiteId]);
  if (!data || !settings) return <Loading />;
  const topSource = data.sources[0];
  const topPage = data.pages[0];
  const topGoal = data.conversionGoals[0];
  const topDevice = data.deviceSegments[0];
  const journey = data.journeys[0];
  const improvements = (settings.improvements ?? []).filter(item => item.status !== "検証").slice(0, 3);
  const changes = (settings.changeLog ?? []).slice(-3).reverse();
  const insight = settings.siteAnalysis?.recommendations?.[0] ?? "サイト分析を実行すると、構成と訴求を踏まえた改善ポイントが表示されます。";
  const conclusion = data.sessions ? `${data.sessions.toLocaleString()}セッションから${data.conversions.toLocaleString()}件の成果を計測しました。${topSource ? `最も成果につながった流入は「${topSource.name}」で、成果率は${topSource.rate}%です。` : ""}` : "今月はまだ十分な計測データがありません。タグの設置状況を確認してください。";
  return <><ScreenHead eyebrow="SHARE" title={`${selectedSite.clientName} Monthly View`} sub="今月の成果、ユーザー行動、改善方針を一つのストーリーで共有します。" /><div className="client-hero"><span>THIS MONTH / EXECUTIVE SUMMARY</span><h2>{data.conversions.toLocaleString()} <small>CONVERSIONS</small></h2><p>{conclusion}</p></div><div className="report-kpis"><article><span>SESSIONS</span><strong>{data.sessions.toLocaleString()}</strong></article><article><span>USERS</span><strong>{data.measuredUsers.toLocaleString()}</strong></article><article><span>CVR</span><strong>{data.conversionRate}%</strong></article><article><span>AVG. ENGAGEMENT</span><strong>{Math.floor(data.averageEngagementSeconds / 60)}:{String(data.averageEngagementSeconds % 60).padStart(2, "0")}</strong></article><article><span>BOUNCE RATE</span><strong>{data.bounceRate}%</strong></article></div><div className="report-analysis-grid"><section className="panel report-ranking"><div className="panel-head"><h3>成果につながった流入</h3><span>ACQUISITION</span></div>{data.sources.slice(0, 5).map(row => <article key={row.name}><b>{row.name}</b><span>{row.sessions.toLocaleString()} sessions</span><strong>{row.outcomes} CV</strong><em>{row.rate}%</em></article>)}{!data.sources.length && <div className="empty-state">流入データがありません</div>}</section><section className="panel report-ranking"><div className="panel-head"><h3>閲覧されたページ</h3><span>CONTENT</span></div>{data.pages.slice(0, 5).map(row => <article key={row.name}><b>{row.name}</b><span>{row.sessions.toLocaleString()} sessions</span><strong>{row.outcomes} CV</strong><em>{row.rate}%</em></article>)}{!data.pages.length && <div className="empty-state">ページデータがありません</div>}</section><section className="panel report-focus"><span>USER BEHAVIOR</span><h3>{topDevice ? `${topDevice.name}が最多の利用環境です。` : "デバイス傾向は集計中です。"}</h3><p>{topDevice ? `${topDevice.sessions.toLocaleString()}セッション、CVR ${topDevice.rate}%を記録しています。` : "計測が進むと、優先して改善すべき端末が表示されます。"}</p><div><b>主要成果</b><strong>{topGoal ? `${topGoal.name}：${topGoal.outcomes}件` : "未計測"}</strong></div></section><section className="panel report-focus insight"><span>ANALYSIS</span><h3>今月の分析ポイント</h3><p>{insight}</p><div><b>計測の信頼度</b><strong>流入判定率 {data.attributionCoverage}%</strong></div></section><section className="panel report-journey"><div className="panel-head"><h3>主要な閲覧導線</h3><span>{journey?.source ?? "JOURNEY"}</span></div>{journey?.pages.length ? <div>{journey.pages.map((page, index) => <article key={`${page.name}-${index}`}><i>{index + 1}</i><b>{page.name}</b><span>{page.sessions.toLocaleString()} sessions</span>{index < journey.pages.length - 1 && <ArrowRight />}</article>)}</div> : <div className="empty-state">導線データがありません</div>}</section><section className="panel report-actions"><div className="panel-head"><h3>次月の改善アクション</h3><span>NEXT ACTION</span></div>{improvements.length ? improvements.map((item, index) => <article key={item.id}><i>{String(index + 1).padStart(2, "0")}</i><div><b>{item.title}</b><p>{item.proposal || item.problem}</p></div><span>{item.status}</span></article>) : <div className="empty-state">改善管理で次の施策を登録してください</div>}</section><section className="panel report-changes"><div className="panel-head"><h3>今月の対応履歴</h3><span>CHANGE LOG</span></div>{changes.length ? changes.map(item => <article key={item.id}><time>{item.date}</time><div><b>{item.title}</b><p>{item.reason}</p></div><strong>{item.result}</strong></article>) : <div className="empty-state">公開済みの変更はまだありません</div>}</section><section className={`panel report-quality ${data.dataQuality.hasConversions ? "good" : "attention"}`}><Sparkle /><div><span>DATA QUALITY</span><h3>{data.dataQuality.hasConversions ? "成果を含むレポートを作成できています。" : "コンバージョン設定を確認してください。"}</h3><p>最終イベント：{data.dataQuality.lastEventAt ? new Date(data.dataQuality.lastEventAt).toLocaleString("ja-JP") : "未受信"} ・ 計測ページ：{data.dataQuality.taggedPages}</p></div></section></div></>;
}

export function AgencyOverviewScreen() {
  const { sites, selectSite } = useSiteWorkspace();
  const clients = useMemo(() => Object.entries(sites.reduce<Record<string, typeof sites>>((all, site) => { (all[site.clientName] ??= []).push(site); return all; }, {})), [sites]);
  return <><ScreenHead eyebrow="AGENCY VIEW" title="Clients" sub="クライアントとHP・LPを一覧で管理します。" /><div className="agency-summary"><article><span>CLIENTS</span><strong>{clients.length}</strong></article><article><span>SITES</span><strong>{sites.length}</strong></article><article><span>NEEDS ATTENTION</span><strong>0</strong></article></div><div className="agency-clients">{clients.map(([client, items]) => <section className="panel" key={client}><div className="panel-head"><h3>{client}</h3><span>{items.length} projects</span></div>{items.map(site => <button key={site.id} onClick={() => selectSite(site.id)}><i /><span><b>{site.name}</b><small>{site.domain}</small></span><em>{site.siteType === "landing_page" ? "LP" : site.siteType === "recruit" ? "RECRUIT" : "HP"}</em><ArrowRight /></button>)}</section>)}</div></>;
}
